# 首页默认选中商品分组功能

## 功能说明

现在您可以通过URL参数来指定首页默认选中的商品分组，而不是总是默认显示"全部"分组。

## 使用方法

### 基本语法
```
/?group_id=分组ID
```

### 示例
1. 默认显示全部商品：
   ```
   /
   ```

2. 默认选中ID为3的商品分组：
   ```
   /?group_id=3
   ```

3. 默认选中ID为5的商品分组：
   ```
   /?group_id=5
   ```

## 参数说明

- `group_id`：商品分组的ID
  - 如果不提供此参数，默认显示"全部"分组
  - 如果提供的分组ID不存在，会自动回退到"全部"分组
  - 特殊值 `all` 等同于不提供参数

## 支持的主题

此功能已在以下主题中实现：

1. **Hyper主题** - 完全支持
2. **Unicorn主题** - 完全支持  
3. **Luna主题** - 完全支持

## 技术实现

### 后端修改
- 修改了 `HomeController@index` 方法，添加了 `group_id` 参数处理
- 验证分组ID的有效性
- 将默认分组ID传递给视图

### 前端修改
- **Hyper & Unicorn主题**：修改了Blade模板，使用条件判断来决定哪个tab默认激活
- **Luna主题**：修改了JavaScript逻辑，根据传入的分组ID选择对应的分组

## 获取分组ID

您可以通过以下方式获取商品分组的ID：

1. 查看数据库中的 `goods_groups` 表
2. 在管理后台的商品分组管理页面查看
3. 在首页点击不同分组时，观察浏览器开发者工具中的元素ID

## 注意事项

1. 分组ID必须是有效的数字ID
2. 如果分组不存在或被禁用，会自动回退到显示全部商品
3. 此功能不影响原有的tab切换功能
4. URL中的参数会在页面刷新后保持
